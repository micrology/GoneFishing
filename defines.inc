<?php/*==================================================            GONE FISHING            v1.0 April 2001            Nigel Gilbert            n.gilbert@soc.surrey.ac.uk====================================================*/$defined = true;/* all global constants are defined here */$admin_mail = "n.gilbert@soc.surrey.ac.uk";  /* where to send error messages in case of DB failures */$maintenance = 123;	// initial value of harbour fees$k1 = 0.0000093;	// relates investment to quantity fished$k2 = 10;		// maximum market price (price when no fish are in market)$k3 = 0.0002;		// slope of demand curve$k4 = 0.061;		// minimum price of fish, even when there is an oversupply$k5 = 0.05;		// growth rate of fish when sea is half stocked$k6 = 200000;		// maximum capacity of sea$updateInterval = 30 * 60;  // number of seconds between updates of the environment$idleTime       = 60 * 60;  // if player does nothing for this time, then log s/he out$phantomName     = "Phantom Agent 1"; 	// the name of the computational agent that goes fishing$phantomActivityRate = 100;    		// %age probability that the phantom fisher will                                         // go fishing after every human user turn (as %)$phantomCode = 'return (($sea->stock > $k6 / 4) && ($this->balance > 2 * $sea->maintenance) ?                         min(5000, round($this->balance / 2)) :                        0);';?>