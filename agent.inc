<?php/*==================================================            GONE FISHING            v1.0 April 2001            Nigel Gilbert            n.gilbert@soc.surrey.ac.uk====================================================*//* carries out the actions of the phantom agentIncluded by index.php*//* note:  phantoms are distinguishable from humans in the people table because they have type set to 'agent' */include("defines.inc");   if (rand(0, 100) <= $phantomActivityRate) {  /*only activate the agent now and then */           $query = new query("SELECT name FROM people WHERE type = 'agent'");        while ($query->next_rec()) {                $phantom = new Phantom();                $phantom->retrieve($query->field('name'));                $phantom->invest();                }    }    ?>